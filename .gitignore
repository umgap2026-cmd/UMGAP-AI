# Secrets / credentials
.env
.env.*
!.env.example
*firebase-adminsdk*.json
*serviceAccount*.json

# Database dumps / backups
*.sql
*.dump

# Caches / build artifacts
__pycache__/
*.pyc
cache/

# Local editor / workspace
*.code-workspace
.claude/settings.local.json
.DS_Store

# Runtime uploads (user-generated, not source)
static/uploads/

# APK rilis mobile -- diupload manual ke VPS via scp (lihat app.py
# app_version()), JANGAN dicommit ke git (binary besar, bukan source code)
static/downloads/
